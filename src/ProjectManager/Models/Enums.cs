namespace ProjectManager.Models;

public enum ProjectStatus
{
    Planned,
    InProgress,
    Completed
}

public enum TimelineEntryType
{
    Start,
    Update,
    Photo,
    Milestone,
    Completion,
    PostCompletion
}
